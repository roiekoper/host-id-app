apiVersion: apps/v1beta1 # for versions before 1.6.0 use extensions/v1beta1
kind: Deployment
metadata:
  name: host-id
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: iftach
    spec:
      containers:
      - name: frontend
        image: iftachsc/traiana:iftach1
        ports:
        - containerPort: 3000